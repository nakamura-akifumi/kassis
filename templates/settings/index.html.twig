{% extends 'base.html.twig' %}

{% block title %}設定{% endblock %}

{% block body %}
<div class="container py-3">
    <div class="d-flex align-items-center justify-content-between mb-3">
        <div>
            <h1 class="h4 mb-1">設定</h1>
            <p class="text-muted small mb-0">各設定画面へ移動できます。</p>
        </div>
        <div class="d-flex gap-2">
            <a class="btn btn-outline-secondary btn-sm" href="{{ path('app_home') }}">ホームへ</a>
        </div>
    </div>

    <div class="row g-3">
        <div class="col-12">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h6">設定一括インポート / エクスポート</h2>
                    <p class="text-muted small mb-2">コード・貸出グループ・貸出条件をまとめて処理します（Excelのみ）。シート名は「コード設定」「貸出グループ」「貸出条件」です。</p>
                    <div class="d-flex flex-wrap gap-2 align-items-center" style="height: 2.5rem;">
                        <a class="btn btn-outline-primary btn-sm px-3 py-2 d-inline-flex align-items-center justify-content-center" style="height: 80%; min-width: 140px;" href="{{ path('app_settings_export') }}">Excel出力</a>
                        <form method="post" action="{{ path('app_settings_import') }}" enctype="multipart/form-data" class="d-flex align-items-center gap-2">
                            <input type="hidden" name="_token" value="{{ csrf_token('settings_import') }}">
                            <input class="form-control form-control-sm" type="file" name="uploadFile" accept=".xlsx" required>
                            <button type="submit" class="btn btn-outline-secondary btn-sm px-3 py-2 d-inline-flex align-items-center justify-content-center" style="height: 80%; min-width: 142px;">インポート</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h6">コード設定</h2>
                    <p class="text-muted small mb-2">コードテーブルの保守とインポート/エクスポート。</p>
                    <a class="btn btn-outline-primary btn-sm" href="{{ path('app_settings_code') }}">コード設定へ</a>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h6">貸出グループ設定</h2>
                    <p class="text-muted small mb-2">分類1を貸出グループにまとめて管理。</p>
                    <a class="btn btn-outline-primary btn-sm" href="{{ path('app_settings_loan_group') }}">貸出グループへ</a>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h6">貸出条件設定</h2>
                    <p class="text-muted small mb-2">利用者グループと貸出グループの貸出条件を管理。</p>
                    <a class="btn btn-outline-primary btn-sm" href="{{ path('app_settings_loan_condition') }}">貸出条件へ</a>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card shadow-sm h-100">
                <div class="card-body">
                    <h2 class="h6">イベントカレンダー管理</h2>
                    <p class="text-muted small mb-2">閉館日やイベントを登録・iCalで連携。</p>
                    <a class="btn btn-outline-primary btn-sm" href="{{ path('app_settings_calendar_events') }}">イベント管理へ</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
